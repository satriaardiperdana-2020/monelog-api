# monelog-api

Go API project for Monelog.

## Status

The repository currently contains the Go module definition. API implementation
and application entry points have not been added yet.

## Requirements

- Go 1.27 or later, as declared in `go.mod`.

## Development

Once Go packages and tests are added, run the test suite from the project root:

```sh
go test ./...
```
